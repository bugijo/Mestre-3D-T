---
trigger: always_on
---

sempre me responda em pt-br
sempre consulte .agent/skills/data/auto_activation.json para decidir quais skills usar automaticamente
quando o pedido envolver UI, telas, layout ou componentes, use frontend-design e core-components
quando o pedido envolver bugs, erros, falhas ou depuração, use debugger, find-bugs, error-detective e fix-review
quando o pedido envolver performance ou otimização, use dx-optimizer
quando o pedido envolver arquitetura, estrutura ou refatoração, use architecture, clean-code e code-reviewer
quando o pedido envolver rpg, campanha, combate, npc, mapa ou sprite, use game-development e content-creator
quando o pedido envolver api, supabase, backend ou auth, use api-patterns
